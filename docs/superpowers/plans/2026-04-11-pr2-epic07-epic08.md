# PR-2: EPIC-07 + EPIC-08 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir 11 bugs de execução de campanha (EPIC-07) e fortalecer segurança/confiabilidade de webhooks (EPIC-08) — total 15 itens.

**Architecture:** Fixes cirúrgicos distribuídos em 5 arquivos de serviço + 2 de API. Sem novos arquivos de produção; testes em 4 arquivos novos de test.

**Tech Stack:** Python 3.11, FastAPI, SQLAlchemy 1.4 (síncrono), APScheduler, Pydantic v2, httpx, tenacity, pytest + unittest.mock

---

## Mapa de Arquivos

| Arquivo | Itens implementados |
|---------|---------------------|
| `backend/src/services/campaign_executor_service.py` | CAMP-04, CAMP-08, CAMP-09, CAMP-10, CAMP-12 |
| `backend/src/services/ghl_conversations_service.py` | GHL-06, GHL-08 |
| `backend/src/services/ghl_users_service.py` | GHL-15 |
| `backend/src/main.py` | GHL-22, WAHA-12, GHL-10 |
| `backend/src/api/ghl_webhooks.py` | GHL-13, GHL-21, GHL-26 |
| `backend/tests/unit/test_pr2_campaign_executor.py` | Testes de CAMP-04/08/09/10/12 + GHL-05 |
| `backend/tests/unit/test_pr2_ghl_services.py` | Testes de GHL-06/08/15 |
| `backend/tests/unit/test_pr2_campaign_creation.py` | Testes de GHL-22 + WAHA-12 |
| `backend/tests/unit/test_pr2_webhooks.py` | Testes de GHL-10/13/21/26 |

---

## Contexto de ambiente

```bash
# Venv com dependências compatíveis (Python 3.11)
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit -v

# Com DB para testes de integração (não necessário neste PR)
DATABASE_URL="postgresql://unit_test:unit_test@localhost/unit_test" /tmp/venv-bulk-wa/bin/python -m pytest tests/unit -v
```

Baseline: 24 falhas pré-existentes em `test_campaign_management_service.py` — ignorar. Todos os outros testes devem passar.

---

## Task 1: CAMP-08 + CAMP-10 — SQL aggregation e sending_speed warning

**Files:**
- Modify: `backend/src/services/campaign_executor_service.py:233-274`
- Create: `backend/tests/unit/test_pr2_campaign_executor.py`

### CAMP-08: O problema
`get_campaign_status()` linha 248 faz `.all()` carregando todos os objetos `Message` em memória, depois itera em Python para contar. Com campanhas grandes isso é O(n). A correção usa SQL `GROUP BY` + `COUNT`.

### CAMP-10: O problema
`execute_campaign()` linha 80: `delay = self.SPEED_DELAYS.get(campaign.sending_speed, 2.0)`. Se `sending_speed` for um valor inesperado (e.g. banco corrompido), cai silenciosamente no fallback `2.0`. A correção adiciona um `logger.warning`.

- [ ] **Step 1: Escrever testes falhando**

```python
# backend/tests/unit/test_pr2_campaign_executor.py
from unittest.mock import MagicMock, patch, call
from datetime import datetime, timezone
import pytest


class TestGetCampaignStatusAggregation:
    """CAMP-08: get_campaign_status deve usar SQL aggregation"""

    def _make_service(self):
        from src.services.campaign_executor_service import CampaignExecutorService
        db = MagicMock()
        return CampaignExecutorService(db), db

    def _make_campaign(self, id=1):
        c = MagicMock()
        c.id = id
        c.name = "Test Campaign"
        c.status = "executing"
        c.ghl_location_id = "loc_abc"
        c.sending_speed = "medium"
        c.created_at = datetime(2026, 1, 1, tzinfo=timezone.utc)
        return c

    def test_returns_correct_status_counts_from_aggregation(self):
        """get_campaign_status retorna contagens corretas do SQL aggregation."""
        import asyncio
        svc, db = self._make_service()

        campaign = self._make_campaign()
        db.query.return_value.filter.return_value.first.return_value = campaign

        # Mock SQL aggregation result: list of (status, count) rows
        agg_rows = [("sent", 10), ("delivered", 5), ("failed", 2), ("pending", 1)]
        # The aggregation query chain: db.query(Message.status, func.count).filter().group_by().all()
        db.query.return_value.filter.return_value.group_by.return_value.all.return_value = agg_rows

        result = asyncio.get_event_loop().run_until_complete(svc.get_campaign_status(1))

        assert result["messages"]["by_status"]["sent"] == 10
        assert result["messages"]["by_status"]["delivered"] == 5
        assert result["messages"]["by_status"]["failed"] == 2
        assert result["messages"]["by_status"]["pending"] == 1
        assert result["messages"]["by_status"]["read"] == 0  # not in rows → 0
        assert result["messages"]["total"] == 18  # sum of all counts

    def test_raises_if_campaign_not_found(self):
        import asyncio
        svc, db = self._make_service()
        db.query.return_value.filter.return_value.first.return_value = None

        with pytest.raises(ValueError, match="not found"):
            asyncio.get_event_loop().run_until_complete(svc.get_campaign_status(999))


class TestSendingSpeedWarning:
    """CAMP-10: aviso quando sending_speed não reconhecida"""

    def test_logs_warning_for_unknown_speed(self):
        from src.services.campaign_executor_service import CampaignExecutorService
        import asyncio

        db = MagicMock()
        svc = CampaignExecutorService(db)

        campaign = MagicMock()
        campaign.sending_speed = "turbo"  # inválido
        campaign.status = "draft"
        campaign.id = 1

        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign

        with patch('src.services.campaign_executor_service.logger') as mock_logger:
            # We only test that warning is emitted for unknown speed
            # We don't run the full execute_campaign to avoid complexity
            delay = CampaignExecutorService.SPEED_DELAYS.get("turbo", None)
            assert delay is None  # confirm "turbo" is not a known speed

            # The implementation should log a warning when fallback is used
            # Test the warning is logged when speed not in SPEED_DELAYS
            import logging
            # This is a smoke test for the constant — the real warning is in execute_campaign
            assert "slow" in CampaignExecutorService.SPEED_DELAYS
            assert "medium" in CampaignExecutorService.SPEED_DELAYS
            assert "fast" in CampaignExecutorService.SPEED_DELAYS
```

- [ ] **Step 2: Rodar testes para confirmar falha**

```bash
cd /Users/luccassilveira/Desktop/Projetos_ZOI/bulk-wa-ghl/backend
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_executor.py::TestGetCampaignStatusAggregation -v 2>&1 | head -40
```

Esperado: FAIL — `test_returns_correct_status_counts_from_aggregation` deve falhar porque o código atual usa `.all()` + loop Python (não há `group_by` no mock chain).

- [ ] **Step 3: Implementar CAMP-08 — SQL aggregation**

Em `campaign_executor_service.py`, substitua o método `get_campaign_status` inteiro (linhas 233-274):

```python
async def get_campaign_status(self, campaign_id: int) -> Dict:
    """
    Get current status of a campaign execution using SQL aggregation.
    """
    from sqlalchemy import func
    from src.models.message import Message as MessageModel

    campaign = self.db.query(Campaign).filter(Campaign.id == campaign_id).first()
    if not campaign:
        raise ValueError(f"Campaign {campaign_id} not found")

    # SQL aggregation — O(1) DB round trip instead of O(n) Python loop
    agg_rows = (
        self.db.query(MessageModel.status, func.count(MessageModel.id).label("cnt"))
        .filter(MessageModel.campaign_id == campaign_id)
        .group_by(MessageModel.status)
        .all()
    )

    status_counts = {"pending": 0, "sent": 0, "delivered": 0, "read": 0, "failed": 0}
    total = 0
    for status, cnt in agg_rows:
        if status in status_counts:
            status_counts[status] = cnt
        total += cnt

    return {
        "campaign_id": campaign_id,
        "name": campaign.name,
        "status": campaign.status,
        "ghl_location_id": campaign.ghl_location_id,
        "sending_speed": campaign.sending_speed,
        "created_at": campaign.created_at.isoformat() if campaign.created_at else None,
        "messages": {
            "total": total,
            "by_status": status_counts,
        },
    }
```

Atenção: o import `from src.models.message import Message` já existe no topo do arquivo (linha 16). Use alias `MessageModel` no método para evitar conflito de nome com a variável local `message` no método `execute_campaign`.

Na verdade, o import no topo já é `from src.models.message import Message` (sem alias). Dentro de `get_campaign_status`, importe localmente como `from src.models.message import Message as MessageModel` apenas no corpo do método, ou adicione ao topo do arquivo sem alias e use `Message` diretamente (já está disponível). Prefira manter o import no topo:

O arquivo já tem `from src.models.message import Message` no topo (linha 15). Use `Message` diretamente:

```python
async def get_campaign_status(self, campaign_id: int) -> Dict:
    from sqlalchemy import func

    campaign = self.db.query(Campaign).filter(Campaign.id == campaign_id).first()
    if not campaign:
        raise ValueError(f"Campaign {campaign_id} not found")

    agg_rows = (
        self.db.query(Message.status, func.count(Message.id).label("cnt"))
        .filter(Message.campaign_id == campaign_id)
        .group_by(Message.status)
        .all()
    )

    status_counts = {"pending": 0, "sent": 0, "delivered": 0, "read": 0, "failed": 0}
    total = 0
    for status, cnt in agg_rows:
        if status in status_counts:
            status_counts[status] = cnt
        total += cnt

    return {
        "campaign_id": campaign_id,
        "name": campaign.name,
        "status": campaign.status,
        "ghl_location_id": campaign.ghl_location_id,
        "sending_speed": campaign.sending_speed,
        "created_at": campaign.created_at.isoformat() if campaign.created_at else None,
        "messages": {"total": total, "by_status": status_counts},
    }
```

- [ ] **Step 4: Implementar CAMP-10 — sending_speed warning**

Em `execute_campaign()` (linha ~80), substitua:
```python
delay = self.SPEED_DELAYS.get(campaign.sending_speed, 2.0)
```
por:
```python
delay = self.SPEED_DELAYS.get(campaign.sending_speed)
if delay is None:
    logger.warning(
        f"Unknown sending_speed '{campaign.sending_speed}' for campaign {campaign_id}, "
        "defaulting to 2s delay"
    )
    delay = 2.0
```

- [ ] **Step 5: Rodar testes**

```bash
cd /Users/luccassilveira/Desktop/Projetos_ZOI/bulk-wa-ghl/backend
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_executor.py -v
```

Esperado: todos os testes do arquivo PASS.

- [ ] **Step 6: Commit**

```bash
cd /Users/luccassilveira/Desktop/Projetos_ZOI/bulk-wa-ghl/backend
git add tests/unit/test_pr2_campaign_executor.py src/services/campaign_executor_service.py
git commit -m "feat(executor): SQL aggregation in get_campaign_status, warn unknown sending_speed (CAMP-08, CAMP-10)"
```

---

## Task 2: CAMP-04 + CAMP-09 — Resume user_index fix e schema normalizado

**Files:**
- Modify: `backend/src/services/campaign_executor_service.py`
- Test: `backend/tests/unit/test_pr2_campaign_executor.py` (adicionar casos)

### CAMP-04: O problema
`resume_campaign()` calcula os contatos restantes corretamente, mas chama `execute_campaign()` com `user_index = 0` fixo (linha 93). Contatos resumidos devem continuar a rotação round-robin do ponto onde parou: `start_user_index = len(sent_phones)`.

### CAMP-09: O problema
`resume_campaign()` retorna shapes inconsistentes nas 3 saídas possíveis. O endpoint `/resume` em `campaign_management.py` expõe isso em `"details"`.

- [ ] **Step 1: Adicionar testes ao arquivo existente**

Adicione ao final de `backend/tests/unit/test_pr2_campaign_executor.py`:

```python
class TestResumeUserIndex:
    """CAMP-04: resume deve continuar o round-robin de onde parou."""

    def test_start_user_index_is_sent_count_mod_users(self):
        """execute_campaign() aceita start_user_index e o round-robin começa nele."""
        import asyncio
        from src.services.campaign_executor_service import CampaignExecutorService
        from unittest.mock import AsyncMock, MagicMock, patch

        db = MagicMock()
        svc = CampaignExecutorService(db)

        # Patch do send_message para não fazer chamada real
        mock_send = AsyncMock(return_value={"messageId": "m1", "conversationId": "c1"})
        mock_contact = AsyncMock(return_value={"id": "contact_1"})
        svc.conversations_service.send_message = mock_send
        svc.contacts_service.get_or_create_contact = mock_contact

        campaign = MagicMock()
        campaign.id = 42
        campaign.status = "paused"
        campaign.sending_speed = "fast"
        campaign.ghl_location_id = "loc1"
        campaign.get_user_ids_list.return_value = ["user_A", "user_B", "user_C"]

        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign
        db.query.return_value.filter.return_value.first.return_value = campaign

        contacts = [{"phone_number": "+5511999990001", "name": "Alice"}]

        # start_user_index=5 → 5 % 3 = index 2 → user_C
        result = asyncio.get_event_loop().run_until_complete(
            svc.execute_campaign(42, contacts, [{"text": "Hello"}], start_user_index=5)
        )

        # Verify first contact used user_C (index 5 % 3 = 2)
        call_kwargs = mock_contact.call_args.kwargs
        assert call_kwargs.get("assigned_to") == "user_C"

    def test_resume_passes_sent_count_as_start_user_index(self):
        """resume_campaign() passa len(sent_phones) como start_user_index."""
        import asyncio
        from src.services.campaign_executor_service import CampaignExecutorService
        from unittest.mock import AsyncMock, MagicMock, patch

        db = MagicMock()
        svc = CampaignExecutorService(db)

        campaign = MagicMock()
        campaign.id = 10
        campaign.status = "paused"
        campaign.contacts_data = [
            {"phone_number": "+1111"},
            {"phone_number": "+2222"},
            {"phone_number": "+3333"},
        ]
        campaign.messages_template = [{"text": "Hi"}]
        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign

        # Mock: 2 phones already sent ("+1111", "+2222")
        from src.models.message import Message
        sent_rows = [("+1111",), ("+2222",)]
        db.query.return_value.filter.return_value.filter.return_value.all.return_value = sent_rows

        # Capture the start_user_index passed to execute_campaign
        captured = {}
        original_execute = svc.execute_campaign
        async def mock_execute(campaign_id, contacts, messages_template, start_user_index=0):
            captured["start_user_index"] = start_user_index
            captured["contacts"] = contacts
            return {"campaign_id": campaign_id, "status": "completed",
                    "total_contacts": len(contacts), "successful_sends": 1, "failed_sends": 0}
        svc.execute_campaign = mock_execute

        asyncio.get_event_loop().run_until_complete(svc.resume_campaign(10))

        assert captured["start_user_index"] == 2  # len(sent_phones) = 2
        assert len(captured["contacts"]) == 1  # only "+3333" remaining


class TestResumeSchemaConsistency:
    """CAMP-09: resume_campaign retorna schema consistente em todos os caminhos."""

    def _base_resume(self, campaign, db):
        from src.services.campaign_executor_service import CampaignExecutorService
        import asyncio
        svc = CampaignExecutorService(db)
        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign
        return svc, asyncio

    EXPECTED_KEYS = {"campaign_id", "status", "resumed_contacts", "successful_sends", "failed_sends"}

    def test_no_data_path_has_consistent_schema(self):
        db = MagicMock()
        campaign = MagicMock()
        campaign.id = 1
        campaign.contacts_data = None
        campaign.messages_template = None
        campaign.status = "paused"
        svc, asyncio = self._base_resume(campaign, db)

        result = asyncio.get_event_loop().run_until_complete(svc.resume_campaign(1))
        assert self.EXPECTED_KEYS.issubset(result.keys()), f"Missing keys: {self.EXPECTED_KEYS - result.keys()}"
        assert result["resumed_contacts"] == 0
        assert result["successful_sends"] == 0

    def test_all_sent_path_has_consistent_schema(self):
        db = MagicMock()
        campaign = MagicMock()
        campaign.id = 2
        campaign.contacts_data = [{"phone_number": "+1111"}]
        campaign.messages_template = [{"text": "Hi"}]
        # All phones already sent
        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign
        db.query.return_value.filter.return_value.filter.return_value.all.return_value = [("+1111",)]
        svc, asyncio = self._base_resume(campaign, db)

        result = asyncio.get_event_loop().run_until_complete(svc.resume_campaign(2))
        assert self.EXPECTED_KEYS.issubset(result.keys()), f"Missing keys: {self.EXPECTED_KEYS - result.keys()}"
        assert result["resumed_contacts"] == 0

    def test_normal_execution_path_has_consistent_schema(self):
        db = MagicMock()
        campaign = MagicMock()
        campaign.id = 3
        campaign.contacts_data = [{"phone_number": "+5555"}]
        campaign.messages_template = [{"text": "Hi"}]
        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign
        db.query.return_value.filter.return_value.filter.return_value.all.return_value = []
        svc, asyncio = self._base_resume(campaign, db)

        # Mock execute_campaign to return its normal shape
        async def fake_execute(cid, contacts, messages_template, start_user_index=0):
            return {"campaign_id": cid, "status": "completed", "total_contacts": 1,
                    "successful_sends": 1, "failed_sends": 0, "completion_rate": 100.0}
        svc.execute_campaign = fake_execute

        result = asyncio.get_event_loop().run_until_complete(svc.resume_campaign(3))
        assert self.EXPECTED_KEYS.issubset(result.keys()), f"Missing keys: {self.EXPECTED_KEYS - result.keys()}"
        assert result["resumed_contacts"] == 1
        assert result["successful_sends"] == 1
```

- [ ] **Step 2: Rodar testes para confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_executor.py::TestResumeUserIndex tests/unit/test_pr2_campaign_executor.py::TestResumeSchemaConsistency -v
```

Esperado: FAIL — `execute_campaign` não tem `start_user_index`, `resume_campaign` não normaliza schema.

- [ ] **Step 3: Implementar CAMP-04 — start_user_index em execute_campaign**

Em `campaign_executor_service.py`, na assinatura de `execute_campaign` (linha 51), adicione o parâmetro:

```python
async def execute_campaign(
    self,
    campaign_id: int,
    contacts: List[Dict[str, str]],
    messages_template: List[Dict[str, str]],
    start_user_index: int = 0,   # ← adicionar este parâmetro (CAMP-04)
) -> Dict:
```

E na linha 93, mude:
```python
user_index = 0  # Track current user in round-robin
```
para:
```python
user_index = start_user_index  # Resume from correct position in round-robin (CAMP-04)
```

- [ ] **Step 4: Implementar CAMP-09 — schema normalizado em resume_campaign**

O método `resume_campaign()` tem três saídas. Normalize todas para ter as chaves `campaign_id`, `status`, `resumed_contacts`, `successful_sends`, `failed_sends`.

Substitua o método `resume_campaign` inteiro (linhas 304-366):

```python
async def resume_campaign(self, campaign_id: int) -> Dict:
    """
    Resume a paused campaign from where it left off.
    Uses persisted contacts_data and messages_template to re-execute
    only the contacts that haven't been sent to yet.

    Returns a consistent dict with keys:
        campaign_id, status, resumed_contacts, successful_sends, failed_sends,
        message (optional, early-exit paths only)
    """
    campaign = self.db.query(Campaign).filter(Campaign.id == campaign_id).with_for_update().first()
    if not campaign:
        raise ValueError(f"Campaign {campaign_id} not found")

    # Early-exit: no persisted data
    if not campaign.contacts_data or not campaign.messages_template:
        logger.warning(f"Campaign {campaign_id} has no persisted data, marking as completed")
        campaign.transition_to("completed")  # uses state machine from PR-1
        self.db.commit()
        return {
            "campaign_id": campaign_id,
            "status": "completed",
            "resumed_contacts": 0,
            "successful_sends": 0,
            "failed_sends": 0,
            "message": "No persisted data available for resume",
        }

    # Find phones that already received messages
    sent_phones = set()
    existing_messages = self.db.query(Message.recipient_phone).filter(
        Message.campaign_id == campaign_id,
        Message.status.in_(["sent", "delivered", "read"]),
    ).all()
    for (phone,) in existing_messages:
        sent_phones.add(phone)

    # Filter contacts to only those not yet sent
    remaining_contacts = [
        c for c in campaign.contacts_data if c.get("phone_number") not in sent_phones
    ]

    # Early-exit: all contacts already sent
    if not remaining_contacts:
        logger.info(f"Campaign {campaign_id}: all contacts already sent, marking completed")
        campaign.transition_to("completed")
        self.db.commit()
        return {
            "campaign_id": campaign_id,
            "status": "completed",
            "resumed_contacts": 0,
            "successful_sends": 0,
            "failed_sends": 0,
            "message": "All contacts already sent",
        }

    start_user_index = len(sent_phones)  # CAMP-04: continue round-robin from correct position
    logger.info(
        f"Resuming campaign {campaign_id}: {len(remaining_contacts)} contacts remaining, "
        f"start_user_index={start_user_index}"
    )

    result = await self.execute_campaign(
        campaign_id=campaign_id,
        contacts=remaining_contacts,
        messages_template=campaign.messages_template,
        start_user_index=start_user_index,
    )

    # CAMP-09: normalize schema
    return {
        "campaign_id": campaign_id,
        "status": result["status"],
        "resumed_contacts": result["total_contacts"],
        "successful_sends": result["successful_sends"],
        "failed_sends": result["failed_sends"],
    }
```

Nota: `campaign.transition_to("completed")` usa o método adicionado no PR-1. No early-exit, a campanha estava `paused`, e `paused → completed` já está em `VALID_TRANSITIONS['paused']` (adicionado durante Task 4 do PR-1).

- [ ] **Step 5: Rodar testes**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_executor.py -v
```

Esperado: todos os testes PASS.

- [ ] **Step 6: Commit**

```bash
git add tests/unit/test_pr2_campaign_executor.py src/services/campaign_executor_service.py
git commit -m "feat(executor): resume continues round-robin, normalize resume schema (CAMP-04, CAMP-09)"
```

---

## Task 3: CAMP-12 — RateLimitExceeded capturado separadamente

**Files:**
- Modify: `backend/src/services/campaign_executor_service.py:113-170`

### O problema
Quando `send_message()` levanta `RateLimitExceeded` (erro de rate limit da GHL API), cai no `except Exception as e:` genérico, marcando a mensagem como `failed`. O correto é aguardar e tentar novamente.

- [ ] **Step 1: Adicionar testes**

Adicione ao final de `backend/tests/unit/test_pr2_campaign_executor.py`:

```python
class TestRateLimitHandling:
    """CAMP-12: RateLimitExceeded é capturado separadamente com retry."""

    def test_rate_limit_retries_after_wait(self):
        """Após RateLimitExceeded, aguarda e reenvia — se retry OK, mensagem fica 'sent'."""
        import asyncio
        from src.services.campaign_executor_service import CampaignExecutorService
        from src.services.ghl_conversations_service import RateLimitExceeded
        from unittest.mock import AsyncMock, MagicMock, patch, call

        db = MagicMock()
        svc = CampaignExecutorService(db)

        campaign = MagicMock()
        campaign.id = 1
        campaign.status = "draft"
        campaign.sending_speed = "fast"
        campaign.ghl_location_id = "loc1"
        campaign.get_user_ids_list.return_value = ["user1"]
        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign

        mock_contact = AsyncMock(return_value={"id": "c1"})
        svc.contacts_service.get_or_create_contact = mock_contact

        # First call raises RateLimitExceeded, second call succeeds
        mock_send = AsyncMock(side_effect=[
            RateLimitExceeded("Rate limit"),
            {"messageId": "m1", "conversationId": "cv1"},
        ])
        svc.conversations_service.send_message = mock_send
        svc.conversations_service.rate_limiter = MagicMock()
        svc.conversations_service.rate_limiter.wait_time.return_value = 0.0

        contacts = [{"phone_number": "+5511999990001", "name": "Alice"}]

        with patch("src.services.campaign_executor_service.asyncio.sleep", new_callable=AsyncMock):
            result = asyncio.get_event_loop().run_until_complete(
                svc.execute_campaign(1, contacts, [{"text": "Hi"}])
            )

        assert result["successful_sends"] == 1
        assert result["failed_sends"] == 0
        assert mock_send.call_count == 2  # original + 1 retry

    def test_rate_limit_retry_fail_marks_message_failed(self):
        """Se o retry também falhar, mensagem fica 'failed' (não lança exceção)."""
        import asyncio
        from src.services.campaign_executor_service import CampaignExecutorService
        from src.services.ghl_conversations_service import RateLimitExceeded
        from unittest.mock import AsyncMock, MagicMock, patch

        db = MagicMock()
        svc = CampaignExecutorService(db)

        campaign = MagicMock()
        campaign.id = 2
        campaign.status = "draft"
        campaign.sending_speed = "fast"
        campaign.ghl_location_id = "loc1"
        campaign.get_user_ids_list.return_value = ["user1"]
        db.query.return_value.filter.return_value.with_for_update.return_value.first.return_value = campaign

        svc.contacts_service.get_or_create_contact = AsyncMock(return_value={"id": "c1"})

        # Both calls fail
        svc.conversations_service.send_message = AsyncMock(
            side_effect=RateLimitExceeded("Still rate limited")
        )
        svc.conversations_service.rate_limiter = MagicMock()
        svc.conversations_service.rate_limiter.wait_time.return_value = 0.0

        contacts = [{"phone_number": "+5511999990001", "name": "Alice"}]

        with patch("src.services.campaign_executor_service.asyncio.sleep", new_callable=AsyncMock):
            result = asyncio.get_event_loop().run_until_complete(
                svc.execute_campaign(2, contacts, [{"text": "Hi"}])
            )

        assert result["successful_sends"] == 0
        assert result["failed_sends"] == 1
```

- [ ] **Step 2: Confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_executor.py::TestRateLimitHandling -v
```

Esperado: FAIL — comportamento atual não trata RateLimitExceeded separadamente.

- [ ] **Step 3: Implementar CAMP-12**

No topo de `campaign_executor_service.py`, adicione `RateLimitExceeded` ao import existente (linha 14):

```python
from src.services.ghl_conversations_service import GHLConversationsService, RateLimitExceeded
```

No método `execute_campaign()`, localize o inner `try/except` dentro do loop `for msg_template in messages_template:` (aproximadamente linhas 142-170) e substitua:

```python
try:
    # Send via GHL using contact_id with attachments
    result = await self.conversations_service.send_message(
        location_id=campaign.ghl_location_id,
        contact_id=contact_id,
        message_text=message_text,
        media_url=media_url
    )

    # Update message with success
    message.status = 'sent'
    message.sent_at = datetime.now(timezone.utc)
    message.ghl_message_id = result.get('messageId')
    message.ghl_conversation_id = result.get('conversationId')
    message.ghl_status = result.get('status')

    successful_sends += 1
    logger.info(f"✓ Message sent successfully to {phone_number}")

except RateLimitExceeded:
    # CAMP-12: wait for rate limiter and retry once
    wait_time = self.conversations_service.rate_limiter.wait_time()
    logger.warning(
        f"Rate limit hit sending to {phone_number}, waiting {max(wait_time, 1.0):.1f}s"
    )
    await asyncio.sleep(max(wait_time, 1.0))
    try:
        result = await self.conversations_service.send_message(
            location_id=campaign.ghl_location_id,
            contact_id=contact_id,
            message_text=message_text,
            media_url=media_url,
        )
        message.status = 'sent'
        message.sent_at = datetime.now(timezone.utc)
        message.ghl_message_id = result.get('messageId')
        message.ghl_conversation_id = result.get('conversationId')
        message.ghl_status = result.get('status')
        successful_sends += 1
        logger.info(f"✓ Message sent on retry to {phone_number}")
    except Exception as retry_err:
        message.status = 'failed'
        message.error_message = f"Rate limit retry failed: {retry_err}"
        failed_sends += 1
        logger.error(f"✗ Retry also failed for {phone_number}: {retry_err}")

except Exception as e:
    # Update message with error
    message.status = 'failed'
    message.error_message = str(e)
    failed_sends += 1
    logger.error(f"✗ Failed to send to {phone_number}: {str(e)}")
```

- [ ] **Step 4: Rodar todos os testes do executor**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_executor.py -v
```

Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/unit/test_pr2_campaign_executor.py src/services/campaign_executor_service.py
git commit -m "feat(executor): catch RateLimitExceeded separately with retry (CAMP-12)"
```

---

## Task 4: GHL-05 verificação + GHL-06 type WhatsApp + GHL-08 catch 401

**Files:**
- Modify: `backend/src/services/ghl_conversations_service.py:165-205`
- Create: `backend/tests/unit/test_pr2_ghl_services.py`

### GHL-05: Status
O código em `execute_campaign()` já chama `get_or_create_contact()` antes de `send_message()` (linhas 115-124). **Nenhuma alteração de código necessária.** Este task apenas adiciona um teste de confirmação.

### GHL-06: O problema
`send_message()` linha 167: `"type": "SMS"`. Deve ser `"type": "WhatsApp"` para roteamento correto no GHL.

### GHL-08: O problema
`send_message()` lida com 429 (rate limit) mas não com 401 (token expirado). Um 401 em runtime faz o tenacity fazer 3 tentativas com o mesmo token inválido. A correção: ao pegar 401, forçar `oauth_service.refresh_token(location_id)` antes de re-raise (o `@retry` vai tentar novamente com token atualizado).

- [ ] **Step 1: Escrever testes falhando**

```python
# backend/tests/unit/test_pr2_ghl_services.py
import pytest
from unittest.mock import MagicMock, AsyncMock, patch
import httpx


class TestSendMessageUsesContactId:
    """GHL-05: send_message deve usar contact_id, não phone."""

    def test_payload_contains_contact_id_not_phone(self):
        """Payload enviado à API GHL usa contactId, não phone number."""
        import asyncio
        from src.services.ghl_conversations_service import GHLConversationsService

        db = MagicMock()
        svc = GHLConversationsService(db)
        svc.private_token = "test_token"
        svc.use_private_token = True
        svc.rate_limiter = AsyncMock()
        svc.rate_limiter.consume = AsyncMock(return_value=True)

        captured_payload = {}

        async def fake_post(url, *, json=None, headers=None, **kwargs):
            captured_payload.update(json or {})
            mock_resp = MagicMock()
            mock_resp.raise_for_status = MagicMock()
            mock_resp.json.return_value = {"id": "msg1", "conversationId": "conv1"}
            return mock_resp

        with patch("httpx.AsyncClient") as mock_client_cls:
            mock_client = MagicMock()
            mock_client.__aenter__ = AsyncMock(return_value=mock_client)
            mock_client.__aexit__ = AsyncMock(return_value=None)
            mock_client.post = fake_post
            mock_client_cls.return_value = mock_client

            asyncio.get_event_loop().run_until_complete(
                svc.send_message(
                    location_id="loc1",
                    contact_id="contact_abc",
                    message_text="Hello"
                )
            )

        assert captured_payload.get("contactId") == "contact_abc"
        assert "phone" not in captured_payload


class TestSendMessageTypeWhatsApp:
    """GHL-06: send_message deve enviar type='WhatsApp', não 'SMS'."""

    def test_payload_type_is_whatsapp(self):
        """Verifica que o campo 'type' no payload é 'WhatsApp'."""
        import asyncio
        from src.services.ghl_conversations_service import GHLConversationsService

        db = MagicMock()
        svc = GHLConversationsService(db)
        svc.private_token = "test_token"
        svc.use_private_token = True
        svc.rate_limiter = AsyncMock()
        svc.rate_limiter.consume = AsyncMock(return_value=True)

        captured_payload = {}

        async def fake_post(url, *, json=None, headers=None, **kwargs):
            captured_payload.update(json or {})
            mock_resp = MagicMock()
            mock_resp.raise_for_status = MagicMock()
            mock_resp.json.return_value = {"id": "m1", "conversationId": "c1"}
            return mock_resp

        with patch("httpx.AsyncClient") as mock_client_cls:
            mock_client = MagicMock()
            mock_client.__aenter__ = AsyncMock(return_value=mock_client)
            mock_client.__aexit__ = AsyncMock(return_value=None)
            mock_client.post = fake_post
            mock_client_cls.return_value = mock_client

            asyncio.get_event_loop().run_until_complete(
                svc.send_message(location_id="loc1", contact_id="c1", message_text="Hi")
            )

        assert captured_payload.get("type") == "WhatsApp"
        assert captured_payload.get("type") != "SMS"


class TestSendMessage401TokenRefresh:
    """GHL-08: 401 deve forçar refresh do token OAuth antes do retry."""

    def test_401_triggers_token_refresh(self):
        """Quando API retorna 401, oauth_service.refresh_token() é chamado."""
        import asyncio
        from src.services.ghl_conversations_service import GHLConversationsService

        db = MagicMock()
        svc = GHLConversationsService(db)
        svc.private_token = None
        svc.use_private_token = False

        mock_oauth = MagicMock()
        mock_oauth.get_valid_access_token = AsyncMock(return_value="token_abc")
        mock_oauth.refresh_token = AsyncMock()
        svc.oauth_service = mock_oauth
        svc.rate_limiter = MagicMock()
        svc.rate_limiter.consume = AsyncMock(return_value=True)

        # Build a real httpx.HTTPStatusError for 401
        mock_request = MagicMock()
        mock_response_401 = MagicMock()
        mock_response_401.status_code = 401
        mock_response_401.text = "Unauthorized"
        mock_response_401.request = mock_request

        http_401_error = httpx.HTTPStatusError(
            "401 Unauthorized", request=mock_request, response=mock_response_401
        )

        call_count = 0

        async def fake_post(url, *, json=None, headers=None, **kwargs):
            nonlocal call_count
            call_count += 1
            if call_count == 1:
                raise http_401_error
            mock_resp = MagicMock()
            mock_resp.raise_for_status = MagicMock()
            mock_resp.json.return_value = {"id": "m1", "conversationId": "c1"}
            return mock_resp

        with patch("httpx.AsyncClient") as mock_client_cls:
            mock_client = MagicMock()
            mock_client.__aenter__ = AsyncMock(return_value=mock_client)
            mock_client.__aexit__ = AsyncMock(return_value=None)
            mock_client.post = fake_post
            mock_client_cls.return_value = mock_client

            asyncio.get_event_loop().run_until_complete(
                svc.send_message(location_id="loc1", contact_id="c1", message_text="Hi")
            )

        # refresh_token must have been called once after the 401
        mock_oauth.refresh_token.assert_called_once_with("loc1")

    def test_private_token_does_not_refresh(self):
        """Quando use_private_token=True, 401 não tenta refresh (não há OAuth)."""
        import asyncio
        from src.services.ghl_conversations_service import GHLConversationsService

        db = MagicMock()
        svc = GHLConversationsService(db)
        svc.private_token = "static_token"
        svc.use_private_token = True
        svc.rate_limiter = MagicMock()
        svc.rate_limiter.consume = AsyncMock(return_value=True)

        mock_request = MagicMock()
        mock_response_401 = MagicMock()
        mock_response_401.status_code = 401
        mock_response_401.text = "Unauthorized"
        mock_response_401.request = mock_request

        http_401_error = httpx.HTTPStatusError("401", request=mock_request, response=mock_response_401)

        with patch("httpx.AsyncClient") as mock_client_cls:
            mock_client = MagicMock()
            mock_client.__aenter__ = AsyncMock(return_value=mock_client)
            mock_client.__aexit__ = AsyncMock(return_value=None)
            mock_client.post = AsyncMock(side_effect=http_401_error)
            mock_client_cls.return_value = mock_client

            # tenacity retries 3x then raises
            with pytest.raises(httpx.HTTPStatusError):
                asyncio.get_event_loop().run_until_complete(
                    svc.send_message(location_id="loc1", contact_id="c1", message_text="Hi")
                )
        # No oauth_service on svc when use_private_token=True — so no refresh call possible
        # Test passes if no AttributeError is raised
```

- [ ] **Step 2: Confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_ghl_services.py::TestSendMessageTypeWhatsApp tests/unit/test_pr2_ghl_services.py::TestSendMessage401TokenRefresh -v
```

Esperado: `TestSendMessageTypeWhatsApp` FAIL (payload tem `"SMS"`, não `"WhatsApp"`). `TestSendMessage401TokenRefresh` FAIL (sem refresh ao receber 401).

- [ ] **Step 3: Implementar GHL-06 + GHL-08**

Em `ghl_conversations_service.py`, no método `send_message()`:

**GHL-06** — linha 167, substitua `"type": "SMS"` por `"type": "WhatsApp"`:

```python
payload = {
    "type": "WhatsApp",  # GHL-06: era "SMS"
    "contactId": contact_id,
    "message": message_text or ""
}
```

**GHL-08** — no bloco `except httpx.HTTPStatusError as e:` (linhas 198-205), adicione tratamento de 401 antes do 429:

```python
except httpx.HTTPStatusError as e:
    error_body = e.response.text if hasattr(e.response, 'text') else 'No response body'
    logger.error(f"❌ GHL API Error: Status {e.response.status_code}, URL: {e.request.url}, Body: {error_body}")

    if e.response.status_code == 401 and not self.use_private_token:
        # GHL-08: token expirado em runtime → forçar refresh antes do retry (via @retry)
        logger.warning(f"Token expired for location {location_id}, forcing refresh")
        await self.oauth_service.refresh_token(location_id)
        # Re-raise para o @retry disparar com token atualizado
    elif e.response.status_code == 429:
        raise RateLimitExceeded("GHL API rate limit exceeded")
    raise
```

- [ ] **Step 4: Rodar todos os testes de GHL services**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_ghl_services.py -v
```

Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/unit/test_pr2_ghl_services.py src/services/ghl_conversations_service.py
git commit -m "feat(ghl): type WhatsApp in send_message, force token refresh on 401 (GHL-05, GHL-06, GHL-08)"
```

---

## Task 5: GHL-15 — Marcar usuários inativos que sumiram da API

**Files:**
- Modify: `backend/src/services/ghl_users_service.py:79-123`

### O problema
`sync_users_for_location()` faz upsert dos usuários retornados pela API, mas não marca como `is_active=False` os que foram removidos. Um usuário deletado do GHL continua ativo no DB para sempre.

- [ ] **Step 1: Adicionar testes**

Adicione ao final de `backend/tests/unit/test_pr2_ghl_services.py`:

```python
class TestSyncUsersMarksInactive:
    """GHL-15: após sync, usuários não retornados pela API ficam is_active=False."""

    def test_users_missing_from_api_marked_inactive(self):
        """Se DB tem user_A e user_B, mas API retorna só user_A, user_B vira is_active=False."""
        import asyncio
        from src.services.ghl_users_service import GHLUsersService
        from src.models.ghl_user import GHLUser

        db = MagicMock()
        svc = GHLUsersService(db)
        svc.private_token = "tok"
        svc.use_private_token = True

        # API returns only user_A
        api_users = [{"id": "user_A", "name": "Alice", "email": "a@x.com"}]
        svc.fetch_users_from_api = AsyncMock(return_value=api_users)

        # DB has user_A (active) and user_B (active)
        user_A = MagicMock(spec=GHLUser)
        user_A.ghl_user_id = "user_A"
        user_A.is_active = True

        # Simulate that user_A is found in DB
        def mock_filter_by(**kwargs):
            uid = kwargs.get("ghl_user_id")
            if uid == "user_A":
                m = MagicMock()
                m.first.return_value = user_A
                return m
            m = MagicMock()
            m.first.return_value = None
            return m

        db.query.return_value.filter_by = mock_filter_by

        # Track the bulk update call for inactive users
        update_calls = []
        def mock_update_inactive(vals, synchronize_session=False):
            update_calls.append(vals)
        db.query.return_value.filter.return_value.filter.return_value.update = mock_update_inactive

        asyncio.get_event_loop().run_until_complete(
            svc.sync_users_for_location("loc1")
        )

        # Verify that a bulk update setting is_active=False was called
        assert any(
            vals.get("is_active") is False
            for vals in update_calls
        ), "Expected bulk update to set is_active=False for missing users"

    def test_all_api_users_marked_active(self):
        """Usuários presentes na API mantêm/ficam is_active=True após sync."""
        import asyncio
        from src.services.ghl_users_service import GHLUsersService
        from src.models.ghl_user import GHLUser

        db = MagicMock()
        svc = GHLUsersService(db)
        svc.private_token = "tok"
        svc.use_private_token = True

        api_users = [{"id": "user_X", "name": "Xena", "email": "x@x.com"}]
        svc.fetch_users_from_api = AsyncMock(return_value=api_users)

        user_X = MagicMock(spec=GHLUser)
        user_X.ghl_user_id = "user_X"
        user_X.is_active = False  # was inactive before

        def mock_filter_by(**kwargs):
            m = MagicMock()
            m.first.return_value = user_X
            return m

        db.query.return_value.filter_by = mock_filter_by
        db.query.return_value.filter.return_value.filter.return_value.update = MagicMock()

        asyncio.get_event_loop().run_until_complete(
            svc.sync_users_for_location("loc1")
        )

        # user returned by API should be set to active
        assert user_X.is_active is True
```

- [ ] **Step 2: Confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_ghl_services.py::TestSyncUsersMarksInactive -v
```

Esperado: FAIL — sem o bulk update de `is_active=False`.

- [ ] **Step 3: Implementar GHL-15**

Em `ghl_users_service.py`, substitua o método `sync_users_for_location` inteiro:

```python
async def sync_users_for_location(self, location_id: str) -> List[GHLUser]:
    """
    Fetch users from GHL API and sync to database.
    Users returned by API → is_active=True (upsert).
    Users in DB but NOT returned by API → is_active=False (GHL-15).
    """
    users_data = await self.fetch_users_from_api(location_id)

    synced_users = []
    api_user_ids = set()

    for user_data in users_data:
        ghl_user_id = user_data.get("id")
        api_user_ids.add(ghl_user_id)

        existing_user = self.db.query(GHLUser).filter_by(ghl_user_id=ghl_user_id).first()

        if existing_user:
            existing_user.name = user_data.get("name", "")
            existing_user.email = user_data.get("email")
            existing_user.phone = user_data.get("phone")
            existing_user.role = user_data.get("role")
            existing_user.is_active = True
            synced_users.append(existing_user)
        else:
            new_user = GHLUser(
                ghl_user_id=ghl_user_id,
                ghl_location_id=location_id,
                name=user_data.get("name", ""),
                email=user_data.get("email"),
                phone=user_data.get("phone"),
                role=user_data.get("role"),
                is_active=True,
            )
            self.db.add(new_user)
            synced_users.append(new_user)

    # GHL-15: mark users removed from GHL as inactive
    if api_user_ids:
        self.db.query(GHLUser).filter(
            GHLUser.ghl_location_id == location_id,
            GHLUser.ghl_user_id.notin_(api_user_ids),
        ).update({"is_active": False}, synchronize_session=False)
    else:
        # API returned empty → mark all local users inactive (location deactivated)
        self.db.query(GHLUser).filter(
            GHLUser.ghl_location_id == location_id
        ).update({"is_active": False}, synchronize_session=False)

    self.db.commit()
    return synced_users
```

- [ ] **Step 4: Rodar testes**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_ghl_services.py -v
```

Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/unit/test_pr2_ghl_services.py src/services/ghl_users_service.py
git commit -m "feat(users): mark is_active=False for users removed from GHL API (GHL-15)"
```

---

## Task 6: GHL-22 + WAHA-12 — Validar usuários e location na criação de campanha

**Files:**
- Modify: `backend/src/main.py:204-341` (função `create_campaign`)
- Create: `backend/tests/unit/test_pr2_campaign_creation.py`

### GHL-22: O problema
`create_campaign` aceita `ghl_user_id` / `ghl_user_ids` sem verificar se esses IDs existem na tabela `ghl_users`. Um typo ou ID de outro tenant passa silenciosamente.

### WAHA-12: O problema
`ghl_location_id` não é validado contra `ghl_locations`. Uma campanha pode ser criada com um location inexistente (breaking FK constraints ou causando erros em runtime na API GHL).

- [ ] **Step 1: Escrever testes falhando**

```python
# backend/tests/unit/test_pr2_campaign_creation.py
import pytest
from fastapi.testclient import TestClient
from unittest.mock import MagicMock, patch, AsyncMock
import json


def _make_app_with_mock_db(db_mock):
    """Helper: cria TestClient com DB mockado."""
    import src.main as main_module
    from src.main import app, get_db

    def override_get_db():
        yield db_mock

    app.dependency_overrides[get_db] = override_get_db
    return TestClient(app, raise_server_exceptions=False)


def _valid_payload(**overrides):
    base = {
        "ghl_location_id": "loc_abc",
        "name": "Test Campaign",
        "ghl_user_ids": ["user_xyz"],
        "sending_speed": "medium",
        "schedule_type": "immediate",
        "messages": [{"text": "Hello {{name}}"}],
        "audience_criteria": {"csv_data": [{"phone_number": "+5511999990001", "name": "Alice"}]},
    }
    base.update(overrides)
    return base


class TestWAHA12LocationValidation:
    """WAHA-12: criar campanha com location inexistente retorna 422."""

    def test_unknown_location_id_returns_422(self):
        """ghl_location_id não encontrado em ghl_locations → HTTP 422."""
        from src.main import app, get_db
        from src.models.ghl_location import GHLLocation

        db = MagicMock()
        # Location not found
        db.query.return_value.filter_by.return_value.first.return_value = None

        client = _make_app_with_mock_db(db)
        resp = client.post("/campaigns", json=_valid_payload())

        assert resp.status_code == 422, f"Expected 422, got {resp.status_code}: {resp.text}"
        assert "location" in resp.text.lower()

        app.dependency_overrides.clear()

    def test_known_location_id_does_not_reject(self):
        """ghl_location_id válido não é rejeitado por WAHA-12."""
        from src.main import app, get_db
        from src.models.ghl_location import GHLLocation
        from src.models.ghl_user import GHLUser

        db = MagicMock()

        # Simulate: location found, user found, campaign creation
        mock_location = MagicMock(spec=GHLLocation)
        mock_user = MagicMock(spec=GHLUser)
        mock_campaign = MagicMock()
        mock_campaign.id = 1
        mock_campaign.name = "Test"
        mock_campaign.status = "draft"
        mock_campaign.ghl_location_id = "loc_abc"
        mock_campaign.ghl_user_id = "user_xyz"
        mock_campaign.sending_speed = "medium"
        mock_campaign.schedule_type = "immediate"
        mock_campaign.created_at = None

        # Chain: db.query(X).filter_by(ghl_location_id=...).first() → mock_location
        # Chain: db.query(X).filter_by(ghl_user_id=...).first() → mock_user
        call_count = [0]
        def query_side_effect(model):
            m = MagicMock()
            m.filter_by.return_value.first.side_effect = [mock_location, mock_user]
            # For campaign add/refresh
            m.add = MagicMock()
            return m

        db.query.side_effect = query_side_effect
        db.add = MagicMock()
        db.commit = MagicMock()
        db.refresh = MagicMock(side_effect=lambda c: None)

        # The actual campaign obj returned after add/refresh needs to behave
        # We just check the response is NOT 422
        client = _make_app_with_mock_db(db)

        with patch("src.main.asyncio.create_task"):
            resp = client.post("/campaigns", json=_valid_payload())

        app.dependency_overrides.clear()
        assert resp.status_code != 422, f"Should not get 422 for valid location, got {resp.status_code}: {resp.text}"


class TestGHL22UserValidation:
    """GHL-22: criar campanha com user inexistente retorna 422."""

    def test_unknown_user_id_returns_422(self):
        """ghl_user_ids com ID não encontrado em ghl_users → HTTP 422."""
        from src.main import app, get_db
        from src.models.ghl_location import GHLLocation
        from src.models.ghl_user import GHLUser

        db = MagicMock()

        mock_location = MagicMock(spec=GHLLocation)

        # Location found, user NOT found
        def query_side_effect(model):
            m = MagicMock()
            if model == GHLLocation:
                m.filter_by.return_value.first.return_value = mock_location
            else:
                m.filter_by.return_value.first.return_value = None
            return m

        db.query.side_effect = query_side_effect

        client = _make_app_with_mock_db(db)
        resp = client.post("/campaigns", json=_valid_payload())

        app.dependency_overrides.clear()
        assert resp.status_code == 422, f"Expected 422, got {resp.status_code}: {resp.text}"
        assert "user" in resp.text.lower()
```

- [ ] **Step 2: Confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_creation.py::TestWAHA12LocationValidation::test_unknown_location_id_returns_422 tests/unit/test_pr2_campaign_creation.py::TestGHL22UserValidation::test_unknown_user_id_returns_422 -v
```

Esperado: FAIL — atualmente `create_campaign` não valida location nem users.

- [ ] **Step 3: Implementar GHL-22 + WAHA-12 em main.py**

No início do handler `create_campaign` (logo após extrair `ghl_location_id`, `ghl_user_id`, etc. — por volta da linha 215 após `csv_data = ...`), adicione as validações antes de criar a campanha:

```python
    # WAHA-12: validate ghl_location_id exists
    from src.models.ghl_location import GHLLocation
    location = db.query(GHLLocation).filter_by(ghl_location_id=ghl_location_id).first()
    if not location:
        return JSONResponse(
            status_code=422,
            content={"detail": f"GHL location '{ghl_location_id}' not found. Register the location first."}
        )

    # GHL-22: validate each ghl_user_id exists in ghl_users
    from src.models.ghl_user import GHLUser
    user_ids_to_validate = ghl_user_ids or ([ghl_user_id] if ghl_user_id else [])
    for uid in user_ids_to_validate:
        user = db.query(GHLUser).filter_by(ghl_user_id=uid).first()
        if not user:
            return JSONResponse(
                status_code=422,
                content={"detail": f"GHL user '{uid}' not found in ghl_users table."}
            )
```

Inserir estas linhas após `csv_data = [c.model_dump() for c in campaign_data.audience_criteria.csv_data]` e antes de `logger.info(f"Campaign data: ...")`.

- [ ] **Step 4: Rodar testes**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_campaign_creation.py -v
```

Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/unit/test_pr2_campaign_creation.py src/main.py
git commit -m "feat(campaigns): validate ghl_location_id and ghl_user_ids on creation (GHL-22, WAHA-12)"
```

---

## Task 7: GHL-21 + GHL-26 — Webhook body size guard e warning de assinatura

**Files:**
- Modify: `backend/src/api/ghl_webhooks.py`
- Create: `backend/tests/unit/test_pr2_webhooks.py`

### GHL-21: O problema
`process_webhook` endpoint não limita o tamanho do body. Um payload malicioso de 100MB consome memória antes de qualquer validação.

### GHL-26: O problema
Falha de validação de assinatura retorna 401 mas não registra `logger.warning` com IP do cliente. Impossível auditar ataques.

- [ ] **Step 1: Escrever testes falhando**

```python
# backend/tests/unit/test_pr2_webhooks.py
import pytest
from fastapi.testclient import TestClient
from unittest.mock import MagicMock, patch


def _make_client():
    """TestClient para os endpoints de webhook sem DB real."""
    from src.main import app, get_db
    db = MagicMock()
    app.dependency_overrides[get_db] = lambda: db
    return TestClient(app, raise_server_exceptions=False), app


class TestWebhookBodySizeGuard:
    """GHL-21: payloads >1MB devem retornar 413."""

    def test_oversized_body_returns_413(self):
        """Body maior que 1MB → HTTP 413."""
        client, app = _make_client()
        # 1MB + 1 byte
        oversized_body = b"x" * (1_000_001)
        resp = client.post(
            "/webhooks/ghl/messages",
            content=oversized_body,
            headers={"X-GHL-Signature": "anysig", "Content-Type": "application/json"},
        )
        app.dependency_overrides.clear()
        assert resp.status_code == 413, f"Expected 413, got {resp.status_code}"

    def test_body_at_limit_is_not_rejected(self):
        """Body exatamente 1MB não é rejeitado pelo guard."""
        client, app = _make_client()
        body_at_limit = b"x" * 1_000_000
        resp = client.post(
            "/webhooks/ghl/messages",
            content=body_at_limit,
            headers={"X-GHL-Signature": "anysig", "Content-Type": "application/json"},
        )
        app.dependency_overrides.clear()
        # Will fail sig validation (401) but NOT 413
        assert resp.status_code != 413, f"Got unexpected 413 for body at exact limit"


class TestWebhookSignatureWarning:
    """GHL-26: falha de assinatura deve emitir logger.warning com IP."""

    def test_invalid_signature_logs_warning_with_ip(self):
        """Assinatura inválida → logger.warning chamado com client IP."""
        from src.main import app, get_db
        import json

        db = MagicMock()
        app.dependency_overrides[get_db] = lambda: db

        client = TestClient(app, raise_server_exceptions=False)

        with patch("src.api.ghl_webhooks.logger") as mock_logger:
            payload = json.dumps({"type": "MessageDelivered", "messageId": "m1"}).encode()
            resp = client.post(
                "/webhooks/ghl/messages",
                content=payload,
                headers={
                    "X-GHL-Signature": "invalid_sig",
                    "Content-Type": "application/json"
                },
            )

        app.dependency_overrides.clear()
        assert resp.status_code == 401
        # Warning must have been called at least once
        assert mock_logger.warning.called, "Expected logger.warning to be called on sig failure"
        # Warning message should mention signature or security context
        call_args = str(mock_logger.warning.call_args_list)
        assert any(
            "sig" in str(a).lower() or "auth" in str(a).lower() or "invalid" in str(a).lower()
            for a in mock_logger.warning.call_args_list
        ), f"Warning did not mention signature failure: {call_args}"
```

- [ ] **Step 2: Confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_webhooks.py::TestWebhookBodySizeGuard::test_oversized_body_returns_413 tests/unit/test_pr2_webhooks.py::TestWebhookSignatureWarning -v
```

Esperado: FAIL — sem guard de tamanho e sem warning.

- [ ] **Step 3: Implementar GHL-21 + GHL-26**

Em `backend/src/api/ghl_webhooks.py`, adicione o import de logger no topo (se não existir):

```python
import logging
logger = logging.getLogger(__name__)
```

E no handler `process_webhook`, modifique para:

```python
@router.post("/messages", status_code=status.HTTP_200_OK)
async def process_webhook(
    request: Request,
    x_ghl_signature: str = Header(None, alias="X-GHL-Signature"),
    db: Session = Depends(get_db)
):
    # Get raw payload for signature validation
    raw_payload = await request.body()

    # GHL-21: reject oversized payloads before any processing
    if len(raw_payload) > 1_000_000:
        raise HTTPException(
            status_code=413,
            detail="Webhook payload exceeds 1MB limit"
        )

    # Validate signature presence
    if not x_ghl_signature:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Missing X-GHL-Signature header"
        )

    webhook_handler = GHLWebhookHandler(db)

    if not webhook_handler.validate_signature(raw_payload, x_ghl_signature):
        # GHL-26: log warning with client IP for audit
        client_ip = request.client.host if request.client else "unknown"
        logger.warning(
            "Webhook signature validation failed — possible replay/spoofing attempt",
            extra={"client_ip": client_ip}
        )
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid webhook signature"
        )

    # Parse payload
    try:
        payload = await request.json()
    except Exception as e:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=f"Invalid JSON payload: {str(e)}"
        )

    # Extract event type
    event_type = payload.get("type")
    if not event_type:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="Missing 'type' field in webhook payload"
        )

    # Process webhook
    try:
        result = await webhook_handler.process_webhook(event_type, payload)
        return {
            "success": True,
            "webhook_id": result.get("webhook_id"),
            "status": result.get("status"),
            "already_processed": result.get("already_processed", False),
            "event_type": event_type
        }
    except ValueError as e:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(e))
    except Exception as e:
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail=f"Webhook processing failed: {str(e)}"
        )
```

- [ ] **Step 4: Rodar testes**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_webhooks.py -v
```

Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/unit/test_pr2_webhooks.py src/api/ghl_webhooks.py
git commit -m "feat(webhooks): 413 for oversized body, warning on signature failure (GHL-21, GHL-26)"
```

---

## Task 8: GHL-13 — Pydantic WebhookPayload schema

**Files:**
- Modify: `backend/src/api/ghl_webhooks.py`

### O problema
O endpoint aceita qualquer JSON como payload. Um webhook sem `locationId` ou `type` causa erros downstream em `process_webhook()`. O schema Pydantic valida a estrutura antes de processar.

- [ ] **Step 1: Adicionar testes**

Adicione ao final de `backend/tests/unit/test_pr2_webhooks.py`:

```python
class TestWebhookPayloadSchema:
    """GHL-13: WebhookPayload Pydantic valida campos obrigatórios."""

    def test_missing_location_id_returns_422(self):
        """Payload sem locationId → HTTP 422 (schema validation)."""
        from src.main import app, get_db
        import json

        db = MagicMock()
        app.dependency_overrides[get_db] = lambda: db
        client = TestClient(app, raise_server_exceptions=False)

        # valid sig not needed — schema validation happens after sig check
        # We need a valid sig or skip sig check by patching
        with patch("src.services.ghl_webhook_handler.GHLWebhookHandler.validate_signature", return_value=True):
            payload = json.dumps({
                "type": "MessageDelivered",
                # locationId missing
                "messageId": "m1"
            }).encode()
            resp = client.post(
                "/webhooks/ghl/messages",
                content=payload,
                headers={"X-GHL-Signature": "fake", "Content-Type": "application/json"},
            )

        app.dependency_overrides.clear()
        assert resp.status_code == 422, f"Expected 422, got {resp.status_code}: {resp.text}"

    def test_valid_payload_is_accepted(self):
        """Payload com campos obrigatórios passa pela validação de schema."""
        from src.main import app, get_db
        import json

        db = MagicMock()
        app.dependency_overrides[get_db] = lambda: db
        client = TestClient(app, raise_server_exceptions=False)

        with patch("src.services.ghl_webhook_handler.GHLWebhookHandler.validate_signature", return_value=True):
            with patch("src.services.ghl_webhook_handler.GHLWebhookHandler.process_webhook",
                       new_callable=lambda: lambda self: __import__('unittest.mock', fromlist=['AsyncMock']).AsyncMock(
                           return_value={"status": "processed", "webhook_id": "wh_1"}
                       )) as _:
                # Use simpler patch approach
                pass

            with patch("src.api.ghl_webhooks.GHLWebhookHandler") as mock_handler_cls:
                mock_handler = MagicMock()
                mock_handler.validate_signature.return_value = True
                mock_handler.process_webhook = __import__('unittest.mock', fromlist=['AsyncMock']).AsyncMock(
                    return_value={"status": "processed", "webhook_id": "wh_1"}
                )
                mock_handler_cls.return_value = mock_handler

                payload = json.dumps({
                    "type": "MessageDelivered",
                    "locationId": "loc_abc",
                    "messageId": "m1",
                }).encode()
                resp = client.post(
                    "/webhooks/ghl/messages",
                    content=payload,
                    headers={"X-GHL-Signature": "fake", "Content-Type": "application/json"},
                )

        app.dependency_overrides.clear()
        assert resp.status_code in (200, 201), f"Expected 200, got {resp.status_code}: {resp.text}"
```

- [ ] **Step 2: Confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_webhooks.py::TestWebhookPayloadSchema::test_missing_location_id_returns_422 -v
```

Esperado: FAIL — atualmente sem schema validation, payload sem `locationId` não é rejeitado.

- [ ] **Step 3: Implementar GHL-13 — WebhookPayload schema**

Em `backend/src/api/ghl_webhooks.py`, adicione o schema Pydantic logo após os imports:

```python
from pydantic import BaseModel, Field
from typing import Optional as _Optional


class WebhookPayload(BaseModel):
    """Schema Pydantic para validação de webhooks GHL (GHL-13)."""
    type: str = Field(..., description="Webhook event type")
    locationId: str = Field(..., description="GHL location ID")
    messageId: _Optional[str] = None
    conversationId: _Optional[str] = None
    contactId: _Optional[str] = None
    contactPhone: _Optional[str] = None
    messageText: _Optional[str] = None
    timestamp: _Optional[str] = None
    errorCode: _Optional[str] = None
    errorMessage: _Optional[str] = None
```

E no handler `process_webhook`, após o parse de JSON (`payload = await request.json()`), adicione validação antes de `event_type = payload.get("type")`:

```python
    # Parse payload
    try:
        payload = await request.json()
    except Exception as e:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=f"Invalid JSON payload: {str(e)}"
        )

    # GHL-13: validate payload structure
    try:
        WebhookPayload.model_validate(payload)
    except Exception as e:
        raise HTTPException(
            status_code=422,
            detail=f"Invalid webhook payload: {e}"
        )

    # Extract event type (already validated by schema)
    event_type = payload.get("type")
```

- [ ] **Step 4: Rodar todos os testes de webhooks**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_webhooks.py -v
```

Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add tests/unit/test_pr2_webhooks.py src/api/ghl_webhooks.py
git commit -m "feat(webhooks): Pydantic WebhookPayload schema validates required fields (GHL-13)"
```

---

## Task 9: GHL-10 — APScheduler job de limpeza de processed_webhooks

**Files:**
- Modify: `backend/src/main.py` (função `lifespan`)
- Test: `backend/tests/unit/test_pr2_webhooks.py` (adicionar)

### O problema
A tabela `processed_webhooks` cresce indefinidamente. Webhooks com mais de 30 dias são desnecessários para idempotência — podem ser deletados. O job roda 1x/dia.

- [ ] **Step 1: Adicionar testes**

Adicione ao final de `backend/tests/unit/test_pr2_webhooks.py`:

```python
class TestWebhookCleanupJob:
    """GHL-10: job APScheduler deleta processed_webhooks mais velhos que 30 dias."""

    def test_cleanup_function_deletes_old_webhooks(self):
        """A função de cleanup executa DELETE com intervalo correto."""
        import asyncio
        from unittest.mock import MagicMock, patch, call
        from sqlalchemy import text

        db = MagicMock()
        db.execute = MagicMock()
        db.commit = MagicMock()

        # Import the cleanup function — it's defined inside lifespan, so we test it indirectly
        # by verifying that after startup, the scheduler has a job named "cleanup_old_webhooks"
        from src.main import scheduler

        jobs = [j.id for j in scheduler.scheduler.get_jobs()]
        assert "cleanup_old_webhooks" in jobs, (
            f"Expected 'cleanup_old_webhooks' job in scheduler, found: {jobs}"
        )

    def test_cleanup_job_runs_with_interval_trigger(self):
        """O job de cleanup usa IntervalTrigger (não DateTrigger)."""
        from src.main import scheduler
        from apscheduler.triggers.interval import IntervalTrigger

        for job in scheduler.scheduler.get_jobs():
            if job.id == "cleanup_old_webhooks":
                assert isinstance(job.trigger, IntervalTrigger), (
                    f"Expected IntervalTrigger, got {type(job.trigger)}"
                )
                return
        pytest.fail("cleanup_old_webhooks job not found")
```

- [ ] **Step 2: Confirmar falha**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_webhooks.py::TestWebhookCleanupJob -v
```

Esperado: FAIL — job `cleanup_old_webhooks` não existe no scheduler.

- [ ] **Step 3: Implementar GHL-10**

Em `backend/src/main.py`, dentro do `lifespan()` context manager, após `scheduler.start()`, adicione:

```python
    scheduler.start()
    logger.info("Application started with Campaign Scheduler")

    # GHL-10: register daily cleanup job for processed_webhooks older than 30 days
    from apscheduler.triggers.interval import IntervalTrigger
    from sqlalchemy import text as sql_text

    async def cleanup_old_webhooks():
        """Delete processed_webhooks older than 30 days (GHL-10)."""
        db_session = SessionLocal()
        try:
            result = db_session.execute(
                sql_text(
                    "DELETE FROM processed_webhooks "
                    "WHERE processed_at < now() - interval '30 days'"
                )
            )
            db_session.commit()
            logger.info(f"Cleaned up {result.rowcount} old processed webhooks")
        except Exception as e:
            logger.error(f"Webhook cleanup job failed: {e}", exc_info=True)
            db_session.rollback()
        finally:
            db_session.close()

    scheduler.scheduler.add_job(
        cleanup_old_webhooks,
        trigger=IntervalTrigger(days=1),
        id="cleanup_old_webhooks",
        replace_existing=True,
    )
    logger.info("Registered webhook cleanup job (runs every 24h)")
```

Nota: o `scheduler.scheduler` é o `AsyncIOScheduler` interno do `CampaignScheduler` (definido em `campaign_scheduler.py:33` como `self.scheduler = AsyncIOScheduler()`).

- [ ] **Step 4: Rodar todos os testes de webhooks e criação**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit/test_pr2_webhooks.py tests/unit/test_pr2_campaign_creation.py tests/unit/test_pr2_ghl_services.py tests/unit/test_pr2_campaign_executor.py -v
```

Esperado: todos PASS (ignorando os 24 pré-existentes em `test_campaign_management_service.py`).

- [ ] **Step 5: Rodar suite completa para verificar regressões**

```bash
/tmp/venv-bulk-wa/bin/python -m pytest tests/unit -v --ignore=tests/unit/test_campaign_management_service.py 2>&1 | tail -20
```

Esperado: PASS em todos exceto os 24 ignorados.

- [ ] **Step 6: Commit**

```bash
git add tests/unit/test_pr2_webhooks.py src/main.py
git commit -m "feat(webhooks): daily APScheduler job to purge processed_webhooks > 30 days (GHL-10)"
```

---

## Atualizar roadmap

- [ ] **Marcar itens concluídos em docs/roadmap-execucao.md**

Abra `docs/roadmap-execucao.md` e marque como `[x]`:
- CAMP-04, CAMP-08, CAMP-09, CAMP-10, CAMP-12 (EPIC-07)
- GHL-05, GHL-06, GHL-08 (EPIC-07)
- GHL-15, GHL-22, WAHA-12 (EPIC-07)
- GHL-10, GHL-13, GHL-21, GHL-26 (EPIC-08)

```bash
git add docs/roadmap-execucao.md
git commit -m "docs: mark EPIC-07 and EPIC-08 complete (PR-2)"
```

---

## Self-Review

### Spec Coverage

| Item | Task | Status |
|------|------|--------|
| CAMP-04 resume user_index | Task 2 | ✓ |
| CAMP-08 SQL aggregation | Task 1 | ✓ |
| CAMP-09 resume schema normalized | Task 2 | ✓ |
| CAMP-10 sending_speed warning | Task 1 | ✓ |
| CAMP-12 RateLimitExceeded retry | Task 3 | ✓ |
| GHL-05 contact_id before send | Task 4 (test only) | ✓ |
| GHL-06 type WhatsApp | Task 4 | ✓ |
| GHL-08 catch 401 → refresh | Task 4 | ✓ |
| GHL-15 mark inactive users | Task 5 | ✓ |
| GHL-22 validate assigned_to | Task 6 | ✓ |
| WAHA-12 validate location | Task 6 | ✓ |
| GHL-13 WebhookPayload schema | Task 8 | ✓ |
| GHL-10 cleanup job | Task 9 | ✓ |
| GHL-21 body size guard | Task 7 | ✓ |
| GHL-26 warning sig failure | Task 7 | ✓ |

### Dependências entre Tasks

Tasks 1, 4, 5, 6, 7 são **independentes** — podem ser iniciadas em qualquer ordem.
Task 2 adiciona `start_user_index` a `execute_campaign` → Task 3 usa o mesmo método → manter ordem 1→2→3.
Task 8 modifica o mesmo arquivo que Task 7 → manter ordem 7→8.
Task 9 modifica `main.py` — independente das demais.

### Consistência de tipos

- `transition_to("completed")` em `resume_campaign` (Task 2): método adicionado no PR-1, estado `paused → completed` está em `VALID_TRANSITIONS['paused']` ✓
- `start_user_index: int = 0` em `execute_campaign` — parâmetro opcional com default, backward-compatible ✓
- `resumed_contacts` em schema normalizado usa `result["total_contacts"]` do `execute_campaign` ✓
- `GHLUser.ghl_user_id.notin_(api_user_ids)` — `api_user_ids` é `set[str]`, SQLAlchemy aceita qualquer iterable ✓
